import { Typography, Box } from "@mui/material";

export default function SchemaPage() {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>Schema</Typography>
      <Typography color="text.secondary">Explore table structure and relationships.</Typography>
    </Box>
  );
}
